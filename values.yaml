# values.yaml

replicaCount: 
  tokenHandler: 2
  mortgage: 1

image:
  tokenHandler:
    repository: nardisync/mortgage-microservice
    tag: latest
  mortgage:
    repository: nardisync/token-handler-microservice
    tag: latest


service:
  enabled: true
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080